# URL API backend (rỗng = dùng proxy Vite dev server)
VITE_API_URL=

# Sentry DSN cho frontend — lấy từ https://sentry.io/ → Settings → Client Keys
# Để trống = tắt Sentry (chỉ bật khi PROD + có DSN)
VITE_SENTRY_DSN=
